function add(x: number, y: number): number {
  return x + y;
}

function sub(x: number, y: number) {
  return x - y;
}

function test() {
  return "test";
}

export { add, sub };
export default test;
